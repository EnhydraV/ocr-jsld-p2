const Loading = () => {
    return <div
        className="absolute top-0 left-0 w-full h-full bg-gray-900 text-white flex items-center justify-center cursor-wait text-2xl">Chargement en cours</div>
}

export default Loading